# Role & Objective

현재 프로젝트는 장문(30페이지)~초장문(200페이지 이상)의 문서를 생성하기 위한 파이프라인을 구축하기 위해 만들어졌습니다.
당신은 AI와 랭그래프를 이용하여 이러한 문서 생성 파이프라인을 전문적으로 구축할 줄 아는 파이선 시니어 엔지니어입니다.

현재 작업 디렉터리에 존재하는 2개의 프로젝트를 통합 및 리팩토링 하여, 모듈 간 결합도가 낮고 견고한 파이프라인으로 재구성 할 예정입니다.

- `poc` 버전(./poc):
  - 로컬에서 기능 검증된 코드로 nvidia-nim 위주의 ai 호출로 구성되어 있음.
  - 429, 503, 504에 대응하여 백오프 리트라이 로직, 로컬 디스크 체크포인트 등 자동 수행을 위해 어지간한건 다 구현되어 있음.
  - 랭체인을 이용한 파이프라인은 정상적으로 동작하고, 분량 자체도 적절하게 나오지만 유연성이 떨어진다는 치명적인 단점이 있음.
- `aistudio` 버전(./aistudio):
  - AI 스튜디오를 통해 작성된 코드. gemini-3.5~3.7-flash 모델을 사용하여 텍스트 생성 및 문장의 퉐리티는 우수.
  - 하지만 쓸데없는 UI 렌더링 코드들이 너무 많고, 실제 전체 파이프라인이 자동화되어 수행되지도 않음.
  - 심지어, 보고서의 목적, 템플릿 등을 선택헤가 해 놓고는, 실제 작성 프롬프트에서는 제대로 이런 부분이 가이드가 반영되지 않았음.
  - 심지어 모든 장에서 각자 독립적으로 참고자료 등을 생성하려고 한다던가, 목차수가 줄어드는 템플릿의 경우에는 부록 및 약어표에다가 시사점 등을 작성하는 등, 생각보다 사용자 의도를 제대로 반영하지 못한 채, 기능만 잔뜩 추가된 상태임.
  - 게다가 보고서 타입별로 목차 구성이나, 문장의 강도 등도 조절되어야 하나, 생각보다 목차 구성이 획일적으로 반영되어, 내용 또한 상관 없는 내용들로 채워지는 경우마저 생겨 있음.

## Requirements & Principles

1. 작업 착수 전 'Implementation Plan'과 'Task List' 아티팩트를 먼저 작성하여 승인을 받을 것. 승인 전에는 코드를 직접 수정하지 않을 것.
2. AI 스튜디오 특유의 복잡한 웹 뷰 생성 코드, HTML/컴포넌트 인젝션 찌꺼기는 전부 제거하고 '순수 텍스트/Markdown' 중심 엔진으로 정제할 것.
3. 추후 Streamlit 등의 웹 UI나 로컬 CLI 어디서든 임포트하여 쓸 수 있도록, 비즈니스 로직과 입출력 인터페이스를 철저히 분리하라.

## Target Architecture & Output Files

- 프로젝트의 구성 자체는 `poc`개발에 사용된 `guideline.md`구성을 따라갈 것임.
- 다만, 이것은 최초 구상 시에 작성된 것임을 감안하고, `poc` 개발이나 `aistudio` 개발 및 테스트중에 추가된 내용은 모두 반영되어 있지 않으므로, `poc` 버전이나 `aistudio` 버전에 추가된 안전장치나 기능들이 있다면 `guideline.md`를 업데이트 해 둬야 함.
- `aistudio` 버전과 같은 gui 구성은 일단 지금 상황에서는 고려하지 않음.

## Deliverables

1. Step 1: Implementation Plan 제출 (각 모듈별 클래스/함수 시그니처 및 데이터 흐름 명시)
2. Step 2: 사용자 승인 후 각 파일 생성 및 단위 테스트 통과 확인
